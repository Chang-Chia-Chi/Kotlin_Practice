{
  "hooks": {
    "preEdit": [
      {
      "command": "/c/Users/maxch/.m2/wrapper/dists/apache-maven-3.9.8/af622e91/bin/mvn compile test-compile",
      "description": "Run Maven compilation check before editing to ensure the baseline has no syntax errors.",
      "stopOnError": true,
      "matcher": ".*\\.kt$"
      }
    ],
    "preToolUse": [
      {
      "command": "if echo \"$CLAUDE_TARGET_FILE\" | grep -qE '(application\\.properties|\\.git/)'; then exit 2; fi",
      "description": "Intercept and block attempts to modify critical infrastructure or build configuration files without explicit permission.",
      "tools": ["Write", "Edit"],
      "stopOnError": true
      }
    ],
    "postToolUse": [
      {
      "command": "jq -r '\"\\(.tool_input.command // .tool_input.code) - \\(.tool_input.description // \\\"No description\\\")\"' >> .claude/audit-log.txt",
      "description": "Build an immutable audit log of all autonomous terminal commands executed by the AI.",
      "tools": ["Bash", "Cmd"],
      "stopOnError": false
      }
    ]
  }
}